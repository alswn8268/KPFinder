# AI 폴더 정리 도우미 고도화 기획 및 기능 명세서

## 문서 정보

| 항목 | 내용 |
|---|---|
| 프로젝트명 | AI 폴더 정리 도우미 |
| 권장 제품명 | 로컬 AI 기반 업무문서 정리·거버넌스 도우미 |
| 문서 목적 | 현재 프로토타입을 현업 적용 가능한 수준으로 고도화하기 위한 기획 및 요구사항 정의 |
| 주요 평가 대응 | 현업적용도, 기술 완성도, 안전성, 사용 편의성, 효과성, 확장성 |
| 대상 사용자 | 일반 사무직 사용자, 부서 문서관리 담당자, 공용 폴더 관리자 |
| 실행 환경 | 개인 또는 부서 업무용 PC에서 로컬 실행 |
| 핵심 원칙 | 외부 전송 금지, AI는 제안만 수행, 사용자 승인 후 적용, 모든 변경 기록, 언제든 복구 가능 |

---

# 1. 프로젝트 개요

## 1.1 한 문장 정의

본 프로젝트는 업무 폴더 안의 문서를 스캔하고, 로컬 AI가 문서 내용을 분석해 조직의 표준에 맞는 폴더 구조를 제안하며, 사용자가 직접 제안을 수정·승인한 경우에만 파일을 안전하게 이동하는 로컬 문서 정리 도구이다.

## 1.2 프로젝트가 해결하려는 문제

실제 업무 폴더에는 다음과 같은 문제가 반복적으로 발생한다.

- 파일이 `새 폴더`, `임시`, `backup`, `최종`, `진짜최종` 등의 이름으로 여러 위치에 흩어져 있다.
- 같은 문서의 복사본과 여러 수정본이 혼재한다.
- 작성자마다 폴더 이름과 파일명 기준이 달라 문서를 찾기 어렵다.
- 어떤 파일이 최신본인지 판단하기 어렵다.
- 정리 기준을 통일하기 어렵고, 수작업 정리에 많은 시간이 든다.
- 민감한 사내 문서를 외부 생성형 AI 서비스에 업로드하기 어렵다.
- 자동 정리 프로그램이 파일을 잘못 옮기거나 삭제할 가능성이 우려된다.

현재 프로젝트는 폴더 스캔, 해시 기반 완전 중복 탐지, 문서 텍스트 추출, 로컬 Ollama 모델을 활용한 요약과 폴더 구조 제안, Before/After 비교, 사용자 승인 후 실제 이동, 버전 기록과 되돌리기 기능을 구현하고 있다.

## 1.3 프로젝트의 핵심 가치

### 보안

문서 내용은 외부 생성형 AI API로 전송하지 않고 사용자의 PC에서 실행 중인 Ollama에만 전달한다. 현재 구현도 `localhost`에서 실행되는 Ollama 서버를 사용하도록 구성되어 있다.

### 사용자 통제

AI는 정리 계획을 제안하지만, 사용자의 승인 없이 파일을 이동하거나 삭제하지 않는다.

### 안전한 변경

적용 전 경로와 충돌을 검사하고, 적용 중 문제가 발생하면 이미 이동한 파일까지 자동으로 복구한다.

### 설명 가능성

AI가 특정 파일을 왜 해당 폴더에 배치했는지 파일명, 문서 요약, 키워드, 기존 규칙을 근거로 보여준다.

### 조직 표준화

AI가 임의의 폴더명을 매번 생성하는 것이 아니라, 부서 또는 조직이 정한 표준 폴더 체계 안에서 분류한다.

### 감사 가능성

누가 어떤 정리 계획을 승인했고, 어떤 파일이 어디에서 어디로 이동했는지 기록한다.

---

# 2. 목표 제품 정의

## 2.1 현재 프로토타입

현재 프로젝트는 다음과 같은 흐름으로 구성되어 있다.

```text
대상 폴더 입력
→ 파일 스캔
→ 완전 중복 탐지
→ 파일별 텍스트 추출
→ 로컬 AI 요약
→ AI 폴더 구조 제안
→ Before/After 확인
→ 사용자 동의
→ 실제 파일 이동
→ 버전 기록
→ 필요 시 되돌리기
```

지원 문서 형식은 현재 `.txt`, `.docx`, `.xlsx`, `.csv`, `.pptx`, `.pdf`이며, HWP와 HWPX는 지원 범위에서 제외되어 있다.

## 2.2 목표 제품

고도화된 제품은 다음 흐름을 가져야 한다.

```text
실행 환경 자동 점검
→ 대상 폴더 및 제외 규칙 설정
→ 읽기 전용 스캔
→ 중복·유사·미지원 문서 분류
→ 조직 표준 템플릿 선택
→ 규칙 기반 1차 분류
→ 필요한 파일만 로컬 AI 분석
→ AI 분류 이유와 신뢰도 표시
→ 사용자가 파일별 제안 수정
→ 최종 폴더 구조 미리보기
→ 안전성 사전 검사
→ 사용자 최종 승인
→ 트랜잭션 방식으로 이동
→ 결과 재검증 및 자동 재스캔
→ 결과 리포트 생성
→ 버전별 복구 및 감사 이력 제공
```

## 2.3 최종 제품 메시지

> AI가 파일을 마음대로 정리하는 프로그램이 아니라, 조직의 문서관리 규칙과 사용자의 판단을 바탕으로 안전한 정리를 지원하는 로컬 업무 도구

---

# 3. 프로젝트 범위

## 3.1 제출 버전 필수 범위

다음 항목은 공모전 제출 버전에 반드시 포함하는 것을 권장한다.

1. 대상 폴더 밖으로 파일이 이동하지 않도록 하는 경로 보호
2. 이동 중 오류 발생 시 자동 복구
3. 기존 파일 덮어쓰기 방지
4. 사용자 파일별 제안 수정 기능
5. 조직 표준 폴더 템플릿
6. 실제 최종 상태를 보여주는 Before/After 화면
7. 적용 후 자동 재스캔
8. 실제 성공·실패·제외 건수 기반 결과 리포트
9. HWP/HWPX 최소 대응
10. Ollama 미실행 시에도 사용할 수 있는 기본 기능
11. 캐시와 분석 이력 관리
12. 자동 테스트와 실패 시나리오 검증
13. 실제 Ollama 연결 환경에서의 시연 검증
14. 설치 및 실행 환경 자동 점검

## 3.2 후속 확장 범위

- 이미지형 PDF OCR
- 민감정보 포함 가능성 탐지
- 보존기간 분류
- 파일명 표준화 제안
- 사내 문서관리시스템 연동
- 부서 템플릿 공유
- 관리자 통계
- 다중 사용자 권한
- 네트워크 드라이브 대응
- 실시간 폴더 감시

## 3.3 명시적 제외 범위

제출 버전에서는 다음 기능을 넣지 않는 것이 좋다.

- 사용자 승인 없는 자동 이동
- 자동 파일 삭제
- 중복 파일 자동 삭제
- 문서 내용 자동 수정
- 외부 생성형 AI API 호출
- 실시간 백그라운드 감시
- 범용 업무 챗봇
- 복잡한 전자결재 기능
- 다중 사용자의 동시 편집

기능 수를 늘리는 것보다, 안전한 파일 이동과 오류 복구를 완성도 있게 구현하는 것이 평가에 더 유리하다.

---

# 4. 사용자 정의

## 4.1 일반 사용자

### 특징

- 개발 지식이 없다.
- 바탕화면이나 부서 공용 폴더에 파일이 많이 쌓여 있다.
- 파일을 정리하고 싶지만 잘못 이동될까 걱정한다.
- AI가 제안한 결과를 직접 확인하고 수정하고 싶다.

### 주요 요구

- 설치와 실행이 쉬워야 한다.
- 정리 전후 상태가 명확해야 한다.
- 잘못 분류된 파일을 직접 수정할 수 있어야 한다.
- 언제든 원래 상태로 되돌릴 수 있어야 한다.

## 4.2 문서관리 담당자

### 특징

- 부서별 표준 폴더 체계를 관리한다.
- 문서 분류 기준을 통일해야 한다.
- 정리 결과와 변경 이력이 필요하다.

### 주요 요구

- 조직 표준 폴더 템플릿을 설정할 수 있어야 한다.
- 템플릿 버전을 관리할 수 있어야 한다.
- 적용 내역을 리포트로 저장할 수 있어야 한다.
- 미지원 문서와 예외 파일을 확인할 수 있어야 한다.

## 4.3 시연 및 평가 담당자

### 주요 관심

- 실제 업무에 사용할 수 있는가
- 민감한 문서가 외부로 나가지 않는가
- AI가 잘못 판단했을 때 통제할 수 있는가
- 파일 손상이나 유실에 대비했는가
- 기능이 실제로 끝까지 동작하는가
- 효과를 수치로 설명할 수 있는가

---

# 5. 핵심 사용 시나리오

## 5.1 기본 정리 시나리오

1. 사용자가 프로그램을 실행한다.
2. 프로그램이 Ollama, AI 모델, 폴더 권한, 저장 공간을 점검한다.
3. 사용자가 정리할 폴더를 선택한다.
4. 제외할 폴더와 확장자를 설정한다.
5. 프로그램이 파일을 변경하지 않고 읽기 전용으로 스캔한다.
6. 완전 중복, 유사 문서, 미지원 문서를 구분한다.
7. 사용자가 조직 표준 템플릿을 선택한다.
8. 프로그램이 규칙과 AI를 이용해 폴더 구조를 제안한다.
9. 사용자가 파일별 제안을 검토한다.
10. 잘못된 제안을 수정하거나 일부 파일을 제외한다.
11. 프로그램이 최종 상태와 충돌 여부를 보여준다.
12. 사용자가 최종 동의한다.
13. 프로그램이 파일을 이동한다.
14. 오류가 발생하면 자동으로 원래 상태를 복구한다.
15. 이동 완료 후 폴더를 다시 스캔한다.
16. 정리 결과와 변경 이력을 리포트로 제공한다.
17. 사용자는 버전 관리 화면에서 적용 작업을 되돌릴 수 있다.

## 5.2 AI 연결 실패 시나리오

1. Ollama가 실행되지 않은 상태에서 프로그램을 시작한다.
2. 환경 점검 화면에 AI 연결 실패가 표시된다.
3. 사용자는 다음 기능을 계속 이용할 수 있다.
   - 파일 스캔
   - 완전 중복 탐지
   - 검색
   - 확장자 통계
   - 기존 폴더 구조 확인
   - 규칙 기반 분류
4. AI가 필요한 기능만 비활성화한다.
5. 사용자가 Ollama를 실행한 뒤 `다시 확인`을 누르면 AI 기능이 활성화된다.

## 5.3 이동 중 오류 시나리오

1. 20개 파일 이동 계획이 승인된다.
2. 8번째 파일에서 권한 오류가 발생한다.
3. 프로그램은 이동을 즉시 중단한다.
4. 이미 이동한 7개 파일을 역순으로 복구한다.
5. 복구 결과를 검증한다.
6. 사용자에게 다음 내용을 표시한다.

```text
적용이 완료되지 않았습니다.
이동 완료 후 복구된 파일: 7개
이동되지 않은 파일: 13개
복구 실패 파일: 0개
원본 폴더 상태가 유지되었습니다.
```

## 5.4 파일 변경 감지 시나리오

1. 사용자가 스캔 후 AI 제안을 검토한다.
2. 그 사이 다른 프로그램에서 원본 문서를 수정한다.
3. 적용 직전에 파일 해시 또는 수정 시각이 달라진 것이 확인된다.
4. 해당 파일은 이동하지 않는다.
5. 사용자에게 재스캔을 요청한다.

---

# 6. 현재 구현의 주요 보완 사항

## 6.1 목적지 경로 검증 부족

현재 `validate_assignments`는 AI가 반환한 원본 경로가 실제 스캔 목록에 존재하는지와 목적지 문자열이 비어 있지 않은지를 중심으로 확인한다. 목적지가 대상 폴더 내부인지, 절대경로인지, `..`를 포함하는지에 대한 검사가 없다.

이에 따라 다음과 같은 위험을 차단해야 한다.

```text
../../다른폴더/문서.pdf
C:\Users\다른사용자\문서.pdf
/home/user/외부문서.pdf
```

## 6.2 부분 이동 시 복구 기록 유실 가능성

현재 파일 이동은 파일들을 순서대로 이동한 후 마지막에 이동 로그를 기록하는 구조다. 이동 중간에 오류가 발생하면 일부 파일은 이미 이동됐지만 로그가 완성되지 않을 수 있다.

개선 버전에서는 이동 전 작업 계획을 먼저 저장하고, 각 파일 이동 결과를 즉시 기록해야 한다.

## 6.3 버전 간 되돌리기 충돌

현재 버전 관리 기능은 적용별 로그를 기록하고 특정 버전을 되돌릴 수 있도록 구성되어 있다. 그러나 이후 버전에서 같은 파일이 다시 이동된 경우, 과거 버전만 임의로 되돌리면 파일 위치가 충돌할 수 있다.

초기 버전에서는 가장 최근 작업부터 순서대로 되돌리게 제한하거나, 버전 간 의존성 검사가 필요하다.

## 6.4 사용자 수정 기능 부족

현재 적용 화면에서는 AI 제안 경로를 확인한 뒤 전체 계획에 동의하는 방식이다. 파일별 제외, 목적지 수정, 폴더명 변경 기능은 제공되지 않는다.

현업에서는 AI의 오분류를 사용자가 쉽게 고칠 수 있어야 한다.

## 6.5 실제 최종 상태와 미리보기 차이

현재 제안된 구조 화면은 AI가 이동 대상으로 배정한 경로를 중심으로 표시한다. 이동하지 않는 파일이나 미지원 파일을 포함한 실제 최종 폴더 상태를 보여주는 방식으로 개선해야 한다.

## 6.6 AI 분석 검증 부족

현재 PR 기록에는 단위 테스트와 샘플 폴더 기반 수동 검증이 수행됐다고 되어 있지만, 실제 Ollama 모델을 연결한 AI 분석 단계는 당시 미검증 상태로 기록되어 있다.

제출 전에는 실제 시연 PC에서 모델 연결, 분석 시간, JSON 응답 안정성, 분류 결과를 반드시 검증해야 한다.

---

# 7. 기능 요구사항

# 7.1 실행 환경 점검

## 기능 ID

`FR-ENV-01`

## 목적

사용자가 프로그램 실행 전에 필요한 설치 상태를 쉽게 확인하게 한다.

## 점검 항목

| 점검 항목 | 표시 예시 |
|---|---|
| Python 또는 실행 패키지 상태 | 정상 |
| 필수 라이브러리 | 정상 |
| Ollama 실행 여부 | 연결됨 |
| 기본 AI 모델 설치 여부 | 설치됨 |
| 대상 폴더 읽기 권한 | 정상 |
| 대상 폴더 쓰기 권한 | 정상 |
| 캐시 저장 위치 | 사용 가능 |
| 여유 저장 공간 | 충분 |
| 한글 폰트 | 정상 |
| 운영체제 | Windows 11 |

## 동작 기준

- 모든 항목은 `정상`, `주의`, `오류` 중 하나로 표시한다.
- 오류 항목에는 해결 방법을 명령어보다 사용자의 행동 중심으로 안내한다.
- Ollama 오류가 있어도 스캔 기능은 사용할 수 있어야 한다.
- `다시 확인` 버튼을 제공한다.
- 환경 점검 결과를 제출용 리포트에 포함할 수 있다.

## 완료 기준

- AI 미설치 상태에서도 프로그램이 종료되지 않는다.
- 사용자가 어떤 항목을 해결해야 하는지 알 수 있다.
- 정상 상태와 오류 상태를 시연할 수 있다.

---

# 7.2 대상 폴더 설정

## 기능 ID

`FR-SCAN-01`

## 설정 항목

- 정리 대상 폴더
- 숨김 파일 포함 여부
- 하위 폴더 포함 여부
- 제외할 폴더
- 제외할 확장자
- 최대 분석 파일 크기
- 심볼릭 링크 처리 여부
- 읽기 전용 분석 여부
- 이전 캐시 사용 여부

## 기본값

- 하위 폴더 포함: 사용
- 숨김 파일: 제외
- 심볼릭 링크: 제외
- 시스템 파일: 제외
- 프로그램 캐시 폴더: 제외
- 실제 이동 전까지 읽기 전용 유지

## 예외 처리

다음 경로는 대상 폴더로 선택하지 못하도록 하는 것이 좋다.

- 드라이브 루트
- 운영체제 폴더
- 사용자 홈 전체
- 프로그램 설치 폴더
- 프로그램 캐시 폴더
- 휴지통 및 시스템 복원 영역

---

# 7.3 파일 스캔

## 기능 ID

`FR-SCAN-02`

## 수집 정보

각 파일에 대해 다음 정보를 수집한다.

| 정보 | 설명 |
|---|---|
| 파일명 | 실제 이름 |
| 상대경로 | 선택한 폴더 기준 경로 |
| 확장자 | 소문자로 정규화 |
| 파일 크기 | 바이트 및 읽기 쉬운 단위 |
| 생성 시각 | 운영체제가 제공하는 경우 |
| 수정 시각 | 마지막 수정 시각 |
| 해시 | SHA-256 |
| 심볼릭 링크 여부 | 링크 파일인지 확인 |
| 읽기 가능 여부 | 권한 확인 |
| 쓰기 가능 여부 | 이동 가능 여부 확인 |
| 지원 상태 | 전체 지원, 부분 지원, 미지원 |
| 분석 상태 | 대기, 완료, 실패, 제외 |

## 요구 동작

- 스캔 과정에서는 파일을 수정하지 않는다.
- 진행률과 현재 처리 중인 파일을 표시한다.
- 사용자가 스캔을 취소할 수 있다.
- 읽을 수 없는 파일은 전체 작업을 중단하지 않고 오류 목록에 기록한다.
- 스캔 후 파일 수, 총 용량, 확장자 분포, 폴더 수를 보여준다.
- 심볼릭 링크는 기본적으로 제외한다.
- 너무 큰 파일은 해시 및 텍스트 분석 정책을 분리할 수 있다.

## 결과 예시

```text
총 파일                  1,240개
총 용량                  8.4GB
분석 지원                1,015개
부분 지원                  92개
미지원                     88개
읽기 오류                  11개
심볼릭 링크                34개
완전 중복 그룹             27개
```

---

# 7.4 문서 내용 추출

## 기능 ID

`FR-EXTRACT-01`

## 지원 단계

| 문서 형식 | 제출 버전 처리 방식 |
|---|---|
| TXT | UTF-8, CP949, EUC-KR 순차 처리 |
| CSV | 주요 행과 열 추출 |
| DOCX | 문단, 표, 제목 추출 |
| XLSX | 시트명, 표 머리글, 주요 셀 추출 |
| PPTX | 슬라이드 제목, 본문, 표 추출 |
| PDF | 텍스트 레이어 추출 |
| HWPX | 본문 XML 추출 또는 부분 지원 |
| HWP | 파일명·경로·메타정보 기반 분류 |
| 이미지 PDF | OCR 필요 상태 표시 |
| 암호화 문서 | 암호화 상태 표시 |
| 손상 문서 | 분석 실패 상태 표시 |

## 대표 텍스트 구성

문서의 앞부분만 잘라 사용하는 대신 다음 항목을 조합한다.

- 파일명
- 기존 상위 폴더명
- 문서 제목
- 첫 부분
- 중간 부분
- 마지막 부분
- 소제목 목록
- 표 머리글
- 시트명
- 슬라이드 제목

## 상태 구분

```text
정상 분석
부분 분석
파일명 기반 분석
OCR 필요
암호화 문서
미지원 문서
손상 문서
용량 초과
```

## 사용자 표시 원칙

`내용 없음`과 `텍스트를 추출하지 못함`을 구분해야 한다.

잘못된 예:

```text
내용 없음
```

개선 예:

```text
이 PDF는 이미지 형태로 저장되어 본문을 읽지 못했습니다.
파일명과 기존 폴더를 기준으로만 임시 분류합니다.
```

---

# 7.5 완전 중복 탐지

## 기능 ID

`FR-DUP-01`

## 판정 기준

파일의 SHA-256 해시가 동일한 경우 완전 중복으로 판단한다.

## 표시 정보

- 중복 그룹 수
- 그룹별 파일 목록
- 파일 크기
- 수정 시각
- 경로
- 정리 가능한 추가 사본 수
- 절감 가능 용량
- 원본 후보

## 안전 원칙

- 자동 삭제하지 않는다.
- 가장 오래된 파일이나 가장 최근 파일을 자동으로 원본이라고 단정하지 않는다.
- `유지 권장`, `중복 사본 가능성`, `사용자 검토 필요` 수준으로 제안한다.
- 삭제 기능을 추가하더라도 별도의 기능과 별도의 승인 절차로 분리한다.

---

# 7.6 유사 문서 및 버전 후보 탐지

## 기능 ID

`FR-DUP-02`

## 구분 기준

### 완전 중복

본문과 파일 내용이 완전히 같다.

### 유사 문서

파일명이 비슷하고 본문 일부가 다르다.

### 관련 문서

같은 업무나 프로젝트와 관련되지만 서로 다른 문서다.

## 화면 예시

| 문서 A | 문서 B | 관계 | 근거 |
|---|---|---|---|
| 영업보고서_v1.docx | 영업보고서_v2.docx | 버전 후보 | 파일명과 요약 유사 |
| 계약서.docx | 계약검토의견.pdf | 관련 문서 | 계약 키워드 공통 |
| 사업계획서.docx | 사업계획서_복사본.docx | 완전 중복 | SHA-256 동일 |

## 원칙

문자열 유사도 값은 참고 지표로 표현해야 한다. 현재 프로젝트의 연관도 계산은 파일명과 AI 요약 문자열을 `difflib`로 비교하는 방식이므로, 의미적으로 완벽한 유사도라고 표현해서는 안 된다.

---

# 7.7 조직 표준 폴더 템플릿

## 기능 ID

`FR-TEMPLATE-01`

## 목적

AI가 매번 다른 이름의 폴더를 생성하는 문제를 막고 조직의 문서관리 기준을 유지한다.

## 템플릿 예시

```text
01_경영지원
02_인사
03_재무·회계
04_계약·법무
05_사업기획
06_영업
07_마케팅·홍보
08_회의록
09_교육자료
10_양식
90_보관
99_미분류
```

## 템플릿 구성

- 템플릿명
- 버전
- 작성자
- 작성일
- 폴더 구조
- 폴더 설명
- 키워드 규칙
- 파일명 규칙
- 확장자 규칙
- 제외 규칙
- 우선순위
- 적용 범위

## 분류 우선순위

```text
강제 제외 규칙
→ 조직 고정 규칙
→ 사용자 지정 규칙
→ 기존 폴더 정보
→ 파일명 기반 분류
→ 문서 내용 기반 AI 분류
→ 미분류 처리
```

## 규칙 예시

| 조건 | 결과 |
|---|---|
| 파일명에 `회의록`, `주간회의` 포함 | `08_회의록` |
| 파일명에 `계약서`, `협약서` 포함 | `04_계약·법무` |
| 확장자가 `.xlsx`이고 본문에 `예산` 포함 | `03_재무·회계/예산` |
| 파일명에 `복사본`, `(1)` 포함 | 중복 검토 대상으로 표시 |
| HWP 본문 분석 불가 | 파일명 기반 임시 분류 후 사용자 확인 |

## 관리 기능

- 기본 템플릿 제공
- 새 템플릿 만들기
- 현재 폴더 구조에서 템플릿 생성
- 템플릿 복사
- 템플릿 가져오기·내보내기
- 템플릿 버전 확인
- 적용 전 템플릿 미리보기

---

# 7.8 로컬 AI 분석

## 기능 ID

`FR-AI-01`

## AI의 역할

- 파일별 1~2문장 요약
- 문서 유형 추정
- 주요 키워드 추출
- 표준 폴더 후보 제안
- 분류 이유 생성
- 신뢰도 등급 생성
- 애매한 문서 표시

## AI가 하지 않아야 하는 일

- 실제 파일 이동
- 파일 삭제
- 대상 폴더 밖 경로 결정
- 조직 템플릿에 없는 임의 카테고리 생성
- 원본·최신본 확정
- 보존기간 최종 판정
- 민감정보 여부 최종 확정

## 입력 정보

```text
파일명
현재 상대경로
확장자
문서 대표 텍스트
조직 템플릿
적용 가능한 규칙
허용된 목적지 폴더 목록
```

## 출력 정보

```text
문서 유형
핵심 요약
주요 키워드
추천 목적지
추천 이유
신뢰도
사용자 검토 필요 여부
```

## 신뢰도 표현

정확한 확률값을 계산할 근거가 충분하지 않다면 다음 세 단계로 표시한다.

- 높음
- 보통
- 낮음

### 높음

파일명, 문서 내용, 조직 규칙이 모두 일치한다.

### 보통

파일명과 문서 내용은 일치하지만 직접적인 조직 규칙이 없다.

### 낮음

본문 추출이 불완전하거나 여러 카테고리로 해석할 수 있다.

## AI 응답 검증

AI 응답은 그대로 적용하지 않고 반드시 다음을 검증한다.

- 정해진 형식인지
- 원본 파일이 실제 스캔 목록에 있는지
- 목적지가 허용된 템플릿 폴더인지
- 목적지가 대상 폴더 내부인지
- 폴더 깊이 제한을 지키는지
- 동일 파일이 여러 목적지에 배정되지 않았는지
- 목적지 문자열이 운영체제에서 유효한지
- 동일 목적지 충돌이 있는지

## AI 실패 시 처리

- 해당 파일만 실패 상태로 기록한다.
- 전체 분석을 중단하지 않는다.
- 실패한 파일만 다시 분석할 수 있게 한다.
- 규칙 기반 또는 파일명 기반 결과로 대체할 수 있다.
- 잘못된 JSON 응답은 사용자에게 그대로 노출하지 않고 이해하기 쉬운 오류로 표시한다.

---

# 7.9 분석 캐시

## 기능 ID

`FR-AI-02`

## 캐시 키 구성

현재 구현은 파일 해시와 크기를 중심으로 요약 결과를 재사용한다.

개선 캐시 키에는 다음이 포함되어야 한다.

```text
파일 해시
파일 크기
AI 모델명
프롬프트 버전
문서 추출기 버전
입력 최대 길이
조직 템플릿 버전
```

## 사용자 기능

- 현재 폴더 캐시 삭제
- 전체 캐시 삭제
- 특정 파일만 재분석
- 캐시 생성 시각 확인
- 사용한 모델 확인
- 캐시 보관 기간 설정
- 분석 요약 저장 안 함 선택

## 개인정보 원칙

- 원문 전체를 캐시에 저장하지 않는다.
- 요약 저장 여부를 사용자가 선택할 수 있다.
- 캐시 저장 위치를 화면에 표시한다.
- 삭제 버튼을 제공한다.
- 이동 경로와 분석 요약의 보관 정책을 분리한다.

---

# 7.10 AI 제안 검토 및 편집

## 기능 ID

`FR-PLAN-01`

## 화면 구성

| 선택 | 현재 경로 | 분석 상태 | 추천 폴더 | 추천 이유 | 신뢰도 | 최종 폴더 | 상태 |
|---|---|---|---|---|---|---|---|
| 선택 | 임시/보고서.docx | 정상 | 영업/실적 | 매출·실적 키워드 | 높음 | 영업/실적 | 적용 |
| 선택 | 계약서.pdf | 정상 | 사업기획 | 제안서로 오인 | 낮음 | 법무/계약 | 사용자 수정 |
| 제외 | 개인메모.txt | 정상 | 기타 | 분류 근거 부족 | 낮음 | 기존 위치 | 제외 |

## 사용자 편집 기능

- 파일별 적용 여부 선택
- 목적지 폴더 직접 수정
- 허용된 폴더 드롭다운 선택
- 새 하위 폴더 생성
- 여러 파일 일괄 변경
- 확장자별 일괄 제외
- 낮은 신뢰도만 보기
- 분석 실패만 보기
- 미지원 문서만 보기
- AI 제안으로 되돌리기
- 사용자 수정 내역 표시
- 기존 위치 유지 선택

## 수정 이력

각 항목에 대해 다음을 기록한다.

```text
AI 추천 목적지
사용자 최종 목적지
수정 여부
수정 시각
수정 사유
```

수정 사유는 선택 사항으로 두되, 평가 시연에서는 한두 건을 직접 수정하는 모습을 보여주는 것이 좋다.

---

# 7.11 최종 Before/After 미리보기

## 기능 ID

`FR-PREVIEW-01`

## 현재 구조

모든 스캔 대상 파일의 현재 경로를 표시한다.

## 최종 구조

다음 파일을 모두 포함해야 한다.

- 이동 예정 파일
- 기존 위치 유지 파일
- 사용자 제외 파일
- 미지원 파일
- 분석 실패 파일
- 충돌로 이름이 변경될 파일
- 이동할 수 없는 파일

## 파일 상태 표시

| 상태 | 의미 |
|---|---|
| 이동 | 새 폴더로 이동 예정 |
| 유지 | 현재 위치 유지 |
| 제외 | 사용자가 적용에서 제외 |
| 검토 필요 | 미지원 또는 낮은 신뢰도 |
| 충돌 | 같은 이름의 파일 존재 |
| 차단 | 안전성 규칙 위반 |
| 오류 | 읽기 또는 권한 문제 |

## 요약 정보

```text
이동 예정               25개
현재 위치 유지           6개
사용자 제외              2개
충돌                     1개
분석 실패                1개
미지원                   3개
안전성 차단              0개
```

---

# 7.12 경로 및 안전성 사전 검사

## 기능 ID

`FR-SAFE-01`

## 검사 항목

### 대상 폴더 이탈 검사

정규화된 최종 경로가 반드시 대상 루트 폴더 내부에 있어야 한다.

### 절대경로 차단

운영체제의 절대경로 형식은 허용하지 않는다.

### 상위 경로 차단

`..`를 이용해 상위 폴더로 이동하는 경로는 허용하지 않는다.

### 심볼릭 링크 검사

원본, 중간 폴더, 목적지에 심볼릭 링크가 있는지 확인한다.

### 잘못된 이름 검사

- Windows 예약 이름
- 사용할 수 없는 문자
- 이름 끝의 공백과 마침표
- 지나치게 긴 경로
- 빈 파일명
- 제어문자

### 대소문자 충돌

Windows처럼 대소문자를 구분하지 않는 환경에서 다음 경로가 충돌하는지 확인한다.

```text
Report.docx
report.docx
```

### 목적지 중복 검사

여러 원본 파일이 동일 목적지를 요청하는지 검사한다.

### 현재 파일 상태 검사

- 원본 존재 여부
- 해시 변경 여부
- 수정 시각 변경 여부
- 파일 잠금 여부
- 읽기 권한
- 목적지 쓰기 권한

## 검사 결과

안전성 오류가 한 건이라도 있는 경우 전체 적용 버튼을 비활성화하거나, 해당 항목을 제외한 뒤 다시 승인하도록 한다.

---

# 7.13 충돌 처리

## 기능 ID

`FR-SAFE-02`

## 충돌 유형

- 목적지에 같은 이름의 파일 존재
- 여러 파일이 같은 목적지 사용
- 대소문자만 다른 파일명
- 원본과 목적지가 동일
- 파일과 폴더 이름이 충돌
- 경로 길이 초과

## 처리 선택

사용자가 다음 중 하나를 선택할 수 있다.

```text
기존 파일 유지하고 새 파일 이름 변경
해당 파일 이동 제외
다른 목적지 선택
두 파일 비교 후 결정
```

## 기본 정책

- 절대 덮어쓰지 않는다.
- 자동 이름 변경 결과를 적용 전에 보여준다.
- `_1`, `_2` 같은 이름을 적용한 뒤에만 보여주는 방식은 피한다.
- 사용자가 승인한 최종 이름과 실제 이동된 이름이 일치해야 한다.

---

# 7.14 안전한 파일 적용

## 기능 ID

`FR-APPLY-01`

## 적용 단계

```text
1. 최종 이동 계획 생성
2. 모든 항목 사전 검사
3. 작업 ID 생성
4. 이동 전 작업 계획 저장
5. 원본 파일 상태 재검증
6. 파일별 이동 실행
7. 각 이동 직후 성공 로그 기록
8. 목적지 파일 해시 확인
9. 전체 결과 검증
10. 적용 완료 상태 기록
11. 폴더 자동 재스캔
12. 결과 리포트 생성
```

## 이동 방식

같은 저장 장치에서는 안전한 이동을 사용한다.

다른 저장 장치로 이동해야 하는 경우에는 다음 절차가 필요하다.

```text
복사
→ 복사본 해시 검증
→ 원본과 동일함 확인
→ 원본 삭제
```

## 작업 상태

```text
준비
검증 완료
적용 중
적용 완료
실패 후 자동 복구 완료
부분 복구
복구 실패
사용자 취소
```

## 사용자 취소

- 파일 이동이 시작되기 전에는 즉시 취소할 수 있다.
- 이동이 시작된 후에는 현재 항목을 완료하고 자동 복구 절차로 전환한다.
- 강제 종료나 전원 중단 후에도 다음 실행에서 미완료 작업을 감지해야 한다.

---

# 7.15 자동 복구

## 기능 ID

`FR-APPLY-02`

## 복구 원칙

이동 중 오류가 발생하면 이미 이동된 파일을 역순으로 복구한다.

## 복구 검증

- 원래 경로에 파일이 복구되었는지
- 파일 해시가 원본과 동일한지
- 목적지에 불필요한 복사본이 남지 않았는지
- 자동 생성한 빈 폴더를 삭제할 수 있는지

## 부분 복구 처리

복구가 일부 실패하면 단순히 “실패”라고 표시하지 않고 다음 정보를 보여준다.

```text
복구 성공               7개
복구 실패               1개
수동 확인 필요 경로     1개
```

## 수동 복구 안내

사용자가 직접 확인해야 하는 파일의 원래 경로, 현재 경로, 파일 해시를 제공한다.

---

# 7.16 적용 후 자동 재스캔

## 기능 ID

`FR-APPLY-03`

## 목적

화면에 이전 경로가 남는 문제를 방지하고 실제 파일 상태와 앱 상태를 일치시킨다.

## 동작

- 적용 완료 직후 대상 폴더를 다시 스캔한다.
- 기존 AI 제안과 승인 체크 상태를 초기화한다.
- 실제 이동된 경로를 화면에 반영한다.
- 계획 수와 실제 이동 수가 다른 경우 경고한다.
- 적용 결과를 기준으로 새 버전을 기록한다.

---

# 7.17 버전 관리 및 되돌리기

## 기능 ID

`FR-VERSION-01`

## 기록 항목

```text
작업 ID
버전 ID
대상 폴더
적용 시각
적용한 템플릿
이동 계획 수
실제 이동 성공 수
실패 수
제외 수
사용자 수정 수
작업 상태
로그 위치
```

## 초기 권장 정책

가장 최근에 적용한 미복구 버전부터 순서대로 되돌릴 수 있도록 한다.

## 임의 버전 복구를 지원하는 경우

다음 검사가 필요하다.

- 이후 버전에서 같은 파일을 이동했는지
- 현재 목적지에 해당 파일이 존재하는지
- 현재 파일 해시가 적용 당시와 같은지
- 원래 위치에 다른 파일이 존재하는지
- 복구가 이후 버전에 미치는 영향

## 복구 충돌 처리

```text
원래 위치에 파일 없음
→ 정상 복구

원래 위치에 동일 해시 파일 존재
→ 중복 생성 없이 완료 처리

원래 위치에 다른 파일 존재
→ 자동 덮어쓰기 금지, 사용자 선택 필요

현재 파일이 적용 이후 수정됨
→ 자동 복구 중단, 사용자 확인 필요
```

## 상태 구분

```text
적용됨
정상 복구됨
부분 복구됨
복구 실패
복구 불가
```

실제 복구 건수가 예상과 다르면 정상 복구 상태로 표시해서는 안 된다.

---

# 7.18 검색 및 필터

## 기능 ID

`FR-SEARCH-01`

## 검색 대상

- 파일명
- 상대경로
- AI 요약
- 키워드
- 문서 유형
- 제안 폴더
- 최종 폴더
- 분석 상태

## 필터

- 확장자
- 파일 크기
- 수정 날짜
- 분석 상태
- 신뢰도
- 중복 여부
- 유사 문서 여부
- 사용자 수정 여부
- 미지원 여부
- 이동 예정 여부

## 빈 결과 처리

검색 결과가 0건일 때 차트나 표 처리 오류가 발생하지 않도록 한다.

화면에는 다음처럼 표시한다.

```text
검색 조건에 맞는 파일이 없습니다.
검색어 또는 필터를 변경해 주세요.
```

---

# 7.19 파일 및 폴더 연관도 시각화

## 기능 ID

`FR-GRAPH-01`

## 목적

문서가 어떤 주제별로 묶일 수 있는지 사용자가 빠르게 확인하도록 돕는다.

## 표현 원칙

- 연관도는 참고 정보임을 명시한다.
- AI 분석 전에는 파일명 중심으로 계산한다.
- AI 분석 후에는 요약 정보도 반영한다.
- 파일 수가 많을 경우 자동으로 계산하지 않는다.
- 그래프 탭 진입 시 사용자 버튼으로 계산하게 할 수 있다.
- 상위 관계만 보여주는 옵션을 제공한다.
- 관계 계산 결과를 캐시한다.

## 대용량 제한

현재 파일 그래프는 400개 초과 시 제한하는 구조가 있으나, 폴더 관계 계산에도 같은 수준의 제한과 성능 보호가 필요하다.

---

# 7.20 결과 리포트

## 기능 ID

`FR-REPORT-01`

## 제공 형식

- 화면 대시보드
- JSON
- 엑셀
- PDF 또는 HTML

## 리포트 구성

### 기본 정보

- 대상 폴더
- 분석 시각
- 사용한 모델
- 사용한 템플릿
- 템플릿 버전
- 프로그램 버전

### 스캔 결과

- 전체 파일 수
- 전체 용량
- 확장자 분포
- 폴더 수
- 미지원 파일 수
- 오류 파일 수

### 분석 결과

- 정상 분석
- 부분 분석
- 파일명 기반 분석
- AI 분석 실패
- 낮은 신뢰도 파일
- 사용자 수정 수

### 중복 결과

- 완전 중복 그룹
- 중복 사본 수
- 절감 가능 용량
- 유사 문서 후보

### 적용 결과

- 이동 계획
- 실제 이동 성공
- 유지
- 제외
- 실패
- 충돌
- 자동 복구 여부

### 변경 이력

| 원래 경로 | AI 제안 | 사용자 최종 결정 | 실제 결과 |
|---|---|---|---|
| 임시/a.docx | 영업/a.docx | 법무/a.docx | 법무/a.docx |

### 안전성 검사

- 차단된 외부 경로
- 경로 충돌
- 변경된 원본
- 권한 오류
- 파일 잠금
- 복구 결과

### Before/After

정리 전과 정리 후의 실제 폴더 구조를 모두 포함한다.

---

# 8. 화면 명세

# 8.1 시작 화면

## 주요 구성

```text
프로젝트 소개
보안 원칙
환경 점검
새 정리 시작
최근 작업
설정
```

## 핵심 안내 문구

> 문서 내용은 이 PC에서만 처리됩니다.  
> 사용자가 최종 승인하기 전에는 어떤 파일도 이동하거나 삭제하지 않습니다.

---

# 8.2 환경 점검 화면

| 항목 | 상태 | 조치 |
|---|---|---|
| Ollama | 오류 | Ollama 실행 후 다시 확인 |
| AI 모델 | 미설치 | 모델 설치 안내 |
| 폴더 권한 | 정상 | 없음 |
| 저장 공간 | 주의 | 여유 공간 확인 |

AI 기능을 사용할 수 없는 경우에도 `AI 없이 시작` 버튼을 제공한다.

---

# 8.3 스캔 설정 화면

- 폴더 선택
- 제외 폴더
- 제외 확장자
- 숨김 파일 설정
- 심볼릭 링크 제외
- 파일 크기 제한
- 스캔 시작

폴더 경로를 직접 입력하게 하기보다 폴더 선택 창을 제공하는 것이 좋다.

---

# 8.4 스캔 결과 대시보드

```text
전체 파일
총 용량
중복 그룹
미지원 문서
읽기 오류
폴더 수
```

하단에는 다음 탭을 제공한다.

```text
파일 목록
중복 파일
유사 문서
현재 폴더 구조
확장자 통계
오류·제외 목록
```

---

# 8.5 분류 기준 화면

- 조직 템플릿 선택
- 템플릿 폴더 구조 확인
- 규칙 보기
- 사용자 규칙 추가
- 미분류 처리 방식 선택
- AI 분석 사용 여부

---

# 8.6 AI 분석 화면

- 분석 진행률
- 완료 파일 수
- 캐시 사용 파일 수
- 실패 파일 수
- 남은 파일 수
- 분석 취소
- 완료된 결과 저장
- 실패 파일 재시도

파일별 분석 시간을 그대로 곱한 단순 예상 시간 대신 실제 처리 속도를 기준으로 추정치를 갱신하는 방식이 좋다.

---

# 8.7 제안 편집 화면

가장 중요한 화면이다.

- 표 형태로 파일별 제안 표시
- 목적지 수정
- 일괄 수정
- 제외
- 상태별 필터
- 신뢰도별 필터
- AI 추천 이유 확인
- 수정 내역 표시

사용자가 AI 제안 하나를 수정하는 장면을 공모전 시연에 포함해야 한다.

---

# 8.8 안전성 검사 화면

```text
안전 검사 완료
이동 가능               25개
충돌                    1개
변경 감지               0개
대상 폴더 이탈          0개
권한 오류               0개
```

오류 항목을 클릭하면 파일별 상세 내용을 보여준다.

---

# 8.9 최종 승인 화면

사용자에게 다음 내용을 명확히 보여준다.

```text
실제로 이동될 파일 수
유지될 파일 수
자동 이름 변경 파일 수
제외 파일 수
예상 생성 폴더 수
복구용 버전 생성 여부
```

최종 확인 문구는 다음과 같이 구체적으로 작성한다.

> 25개 파일을 위에 표시된 최종 경로로 이동합니다.  
> 기존 파일은 덮어쓰지 않으며, 오류가 발생하면 이미 이동한 파일을 자동으로 복구합니다.  
> 적용 후 버전 관리 화면에서 이 작업을 되돌릴 수 있습니다.

---

# 8.10 적용 결과 화면

성공 메시지만 표시하지 말고 실제 결과를 구분한다.

```text
이동 성공               24개
변경되어 제외            1개
실패                     0개
자동 복구                해당 없음
새로 생성된 폴더         6개
```

제공 버튼:

- 결과 리포트 보기
- 결과 다운로드
- 정리 후 폴더 열기
- 버전 관리
- 새로 스캔

---

# 8.11 버전 관리 화면

| 버전 | 시각 | 이동 성공 | 상태 | 동작 |
|---|---|---:|---|---|
| v3 | 2026-09-01 15:30 | 24 | 적용됨 | 되돌리기 |
| v2 | 2026-09-01 14:10 | 5 | 복구됨 | 상세 보기 |
| v1 | 2026-08-31 17:20 | 18 | 적용됨 | 이후 버전 때문에 제한 |

각 버전의 파일별 이동 내역과 복구 가능 여부를 확인할 수 있어야 한다.

---

# 9. 데이터 및 상태 명세

# 9.1 스캔 작업

## 주요 정보

```text
스캔 ID
대상 루트 경로
시작 시각
종료 시각
스캔 상태
파일 수
오류 수
제외 규칙
```

# 9.2 파일 정보

```text
파일 ID
절대경로
상대경로
파일명
확장자
크기
생성 시각
수정 시각
해시
지원 상태
분석 상태
요약
문서 유형
키워드
```

# 9.3 정리 계획 항목

```text
파일 ID
원래 상대경로
AI 제안 경로
사용자 최종 경로
이동 여부
추천 이유
신뢰도
검사 결과
충돌 상태
```

# 9.4 적용 작업

```text
작업 ID
스캔 ID
계획 생성 시각
승인 시각
적용 시작 시각
적용 종료 시각
전체 계획 수
성공 수
실패 수
제외 수
작업 상태
```

# 9.5 파일별 적용 로그

```text
원본 경로
목적지 경로
적용 전 해시
적용 후 해시
시작 시각
완료 시각
결과
오류 내용
복구 여부
```

# 9.6 작업 상태 흐름

```text
초기 상태
→ 스캔 중
→ 스캔 완료
→ 분석 중
→ 제안 생성
→ 사용자 편집
→ 안전 검사 완료
→ 승인 완료
→ 적용 중
→ 적용 완료
```

오류 상태:

```text
분석 일부 실패
안전 검사 실패
적용 실패 후 복구 완료
적용 실패 후 부분 복구
복구 실패
사용자 취소
```

---

# 10. 비기능 요구사항

# 10.1 보안

- 외부 생성형 AI API를 호출하지 않는다.
- 문서 원문을 외부 서버로 전송하지 않는다.
- 목적지 경로는 대상 루트 내부로 제한한다.
- 시스템 폴더 접근을 제한한다.
- 심볼릭 링크는 기본 제외한다.
- 기존 파일을 자동 덮어쓰지 않는다.
- 캐시와 로그의 저장 위치를 공개한다.
- 캐시 삭제 기능을 제공한다.
- 민감한 문서 요약 저장 여부를 설정할 수 있다.
- 로그에는 원문을 저장하지 않는 것을 기본으로 한다.

# 10.2 안정성

- 이동 중 오류가 발생해도 원본 상태를 최대한 유지한다.
- 작업 계획을 이동 시작 전에 저장한다.
- 파일별 이동 결과를 즉시 기록한다.
- 프로그램이 강제 종료돼도 미완료 작업을 감지한다.
- 적용 후 실제 폴더 상태를 재검증한다.
- 계획 개수와 실제 결과 개수를 구분한다.

# 10.3 사용성

- 개발 지식이 없는 사용자가 이용할 수 있어야 한다.
- 명령어 입력을 최소화한다.
- 오류 메시지는 원인과 해결 행동을 함께 제공한다.
- 사용자가 AI 제안을 쉽게 수정할 수 있어야 한다.
- 적용 전 실제 결과를 예측할 수 있어야 한다.
- 모든 위험 작업에는 명시적 승인이 필요하다.

# 10.4 성능

성능은 일반적인 약속보다 실제 시연 PC에서 측정한 수치를 제출하는 것이 중요하다.

측정 대상은 다음과 같다.

- 파일 목록 스캔 시간
- 해시 계산 시간
- 문서별 텍스트 추출 시간
- 최초 AI 분석 시간
- 캐시 사용 재분석 시간
- 폴더 구조 제안 시간
- 적용 시간
- 되돌리기 시간
- 그래프 계산 시간

특히 AI 분석은 모든 파일을 순차적으로 처리하기보다 다음 방식을 사용해야 한다.

```text
규칙으로 확실히 분류 가능한 파일 제외
→ 파일명만으로 높은 확신을 얻는 파일 제외
→ 애매한 파일만 AI 본문 분석
```

# 10.5 호환성

우선 지원 환경은 실제 사용자 환경을 고려해 Windows로 정하는 것이 좋다.

검증 항목:

- Windows 한글 경로
- 공백이 있는 경로
- 긴 파일명
- 한글·영문 혼합 파일명
- 네트워크 드라이브
- OneDrive 동기화 폴더
- 파일이 다른 프로그램에서 열려 있는 상태
- 읽기 전용 파일
- 대소문자 충돌

# 10.6 유지관리성

- 스캔, 분석, 정책, 이동, 버전, 리포트를 별도 책임으로 분리한다.
- 설정값을 코드에 직접 고정하지 않는다.
- 템플릿과 프롬프트에 버전을 부여한다.
- 테스트를 자동 실행한다.
- 프로그램 버전과 모델 버전을 리포트에 기록한다.
- 의존성 버전을 고정하거나 잠금 파일을 사용한다.

---

# 11. 오류 처리 명세

| 오류 상황 | 사용자 메시지 | 시스템 동작 |
|---|---|---|
| Ollama 미실행 | AI에 연결할 수 없습니다 | AI 외 기능 사용 허용 |
| 모델 미설치 | 선택한 모델이 설치되지 않았습니다 | 설치 안내 및 모델 변경 |
| 폴더 없음 | 선택한 폴더를 찾을 수 없습니다 | 스캔 중단 |
| 읽기 권한 없음 | 파일을 읽을 권한이 없습니다 | 해당 파일 제외 |
| 파일 잠금 | 다른 프로그램에서 사용 중입니다 | 해당 파일 제외 |
| AI 응답 오류 | 분류 결과를 읽지 못했습니다 | 해당 파일 재시도 |
| 목적지 이탈 | 대상 폴더 밖의 경로입니다 | 적용 차단 |
| 기존 파일 충돌 | 같은 이름의 파일이 있습니다 | 사용자 선택 요청 |
| 원본 변경 | 스캔 이후 파일이 변경되었습니다 | 적용 제외 및 재스캔 |
| 이동 실패 | 파일 이동에 실패했습니다 | 전체 이동 중단 및 복구 |
| 복구 일부 실패 | 일부 파일을 자동 복구하지 못했습니다 | 수동 복구 목록 제공 |
| 캐시 손상 | 이전 분석 결과를 읽지 못했습니다 | 캐시 무시 후 재분석 |
| 저장 공간 부족 | 작업을 완료할 공간이 부족합니다 | 적용 차단 |

오류 메시지는 기술적인 예외 문자열을 그대로 보여주지 않고 사용자가 해야 할 행동을 중심으로 작성한다.

---

# 12. 테스트 명세

# 12.1 단위 테스트

## 스캔

- 일반 파일 탐지
- 숨김 파일 제외
- 제외 폴더 적용
- 읽기 권한 오류
- 심볼릭 링크 제외
- 한글 경로
- 긴 경로

## 해시

- 동일 파일 동일 해시
- 다른 파일 다른 해시
- 읽기 중 오류 처리
- 대용량 파일 스트리밍 처리

## 문서 추출

- 빈 TXT
- CP949 TXT
- 표만 있는 DOCX
- 여러 시트 XLSX
- 빈 PPTX
- 이미지형 PDF
- 암호화 PDF
- HWPX
- 손상 파일

## AI 응답

- 정상 응답
- JSON이 아닌 응답
- 필수 항목 누락
- 잘못된 자료형
- 허용되지 않은 폴더
- 중복 배정
- 절대경로
- 상위 경로
- 매우 긴 경로

## 파일 이동

- 정상 이동
- 동일 경로 제외
- 목적지 충돌
- 원본 없음
- 권한 오류
- 5번째 파일에서 강제 실패
- 실패 후 전체 복구
- 복구 중 충돌
- 해시 불일치

## 버전 관리

- 버전 순차 생성
- 최신 버전 복구
- 이후 버전이 있는 과거 버전 복구 제한
- 부분 복구 상태
- 잘못된 로그
- 로그 파일 누락

# 12.2 통합 테스트

- 스캔부터 AI 제안까지
- AI 제안 수정부터 적용까지
- 적용 후 자동 재스캔
- 적용 후 리포트 생성
- 적용 후 되돌리기
- Ollama 연결 실패 후 재연결
- 캐시 사용 후 모델 변경
- 조직 템플릿 변경 후 재분석

# 12.3 화면 테스트

- 검색 결과 0건
- 파일 0개 폴더
- 파일 1개 폴더
- 파일 400개 초과 그래프
- 분석 취소
- 재시도
- 일괄 편집
- 충돌 처리
- 오류 항목 상세 보기
- 적용 버튼 활성화 조건

# 12.4 보안 테스트

- `../` 경로 차단
- `..\` 경로 차단
- Windows 절대경로 차단
- Unix 절대경로 차단
- 심볼릭 링크를 통한 외부 경로 차단
- 대소문자 충돌
- 예약 파일명
- 경로 길이 초과
- 문서 본문에 포함된 명령형 지시 무시
- AI가 허용되지 않은 목적지를 반환하는 경우 차단

# 12.5 장애 복구 테스트

- 파일 이동 중 프로그램 종료
- 로그 기록 중 프로그램 종료
- 전원 중단을 가정한 미완료 상태
- 원본 파일 변경
- 목적지 파일 변경
- 복구 중 권한 오류
- 복구 후 해시 검증

# 12.6 실제 시연 환경 검증

현재 테스트 외에 반드시 다음을 실제 PC에서 검증한다.

- 실제 Ollama 연결
- 실제 모델 응답
- 30~50개 문서 최초 분석
- 동일 데이터 재분석
- 한글 파일명
- HWP 포함 폴더
- 이미지 PDF 포함 폴더
- 파일 잠금 상태
- 실제 적용과 복구
- 화면 녹화 환경에서의 성능

---

# 13. 현업 적용도 강화 방안

## 13.1 규칙과 AI의 역할 분리

현업에서는 AI가 모든 판단을 하게 하면 결과가 일관되지 않을 수 있다.

따라서 다음 역할 분리가 적절하다.

```text
조직 규칙: 반드시 지켜야 하는 기준
사용자 규칙: 부서 또는 개인이 추가한 기준
AI: 규칙만으로 분류하기 어려운 문서의 보조 판단
사용자: 최종 결정
```

## 13.2 HWP 대응

제출 버전에서 HWP 완전 지원이 어렵더라도 다음 수준은 필요하다.

- HWP 파일을 누락하지 않고 목록에 표시
- 파일명과 기존 경로 기반으로 임시 분류
- 본문 미분석 상태를 명확히 표시
- 사용자가 직접 목적지를 지정
- HWPX는 가능한 범위에서 본문 추출
- 향후 HWP 변환 연동 계획 제시

## 13.3 설치 편의성

현업 적용도 평가에서는 사용자가 직접 Python 명령어를 실행해야 하는 구조가 감점 요인이 될 수 있다.

권장 배포 형태:

- Windows 실행 파일
- 설치 프로그램
- 시작 메뉴 바로가기
- 최초 실행 환경 점검
- Ollama 설치 여부 안내
- 샘플 폴더 자동 생성
- 데모 모드

## 13.4 조직 적용 방식

첫 단계부터 전사 적용을 목표로 하지 않는 것이 좋다.

권장 도입 순서:

```text
1단계: 샘플 데이터 검증
2단계: 개인 테스트 폴더
3단계: 한 부서의 복사본 폴더
4단계: 실제 업무 폴더의 읽기 전용 분석
5단계: 사용자 승인 기반 제한 적용
6단계: 표준 템플릿 확산
```

## 13.5 안전 적용 모드

실제 파일 이동이 부담스러운 조직을 위해 다음 모드를 제공하면 좋다.

### 분석 전용 모드

파일을 전혀 이동하지 않고 리포트만 생성한다.

### 복사 모드

원본은 유지하고 새로운 정리 폴더에 복사한다.

### 이동 모드

검증과 승인 후 실제 파일을 이동한다.

현업 초기 도입에서는 분석 전용 또는 복사 모드가 적합하다.

---

# 14. 효과 측정 지표

공모전에서는 “편리하다”는 설명보다 실제 수치가 중요하다.

## 14.1 정리 효과

| 지표 | 계산 방식 |
|---|---|
| 분류 완료율 | 최종 목적지가 결정된 파일 ÷ 전체 파일 |
| 미분류율 | 검토 필요 파일 ÷ 전체 파일 |
| AI 자동 수용률 | 수정 없이 승인한 AI 제안 ÷ AI 제안 전체 |
| 사용자 수정률 | 사용자가 수정한 제안 ÷ AI 제안 전체 |
| 적용 성공률 | 실제 이동 성공 ÷ 최종 이동 계획 |
| 복구 성공률 | 정상 복원 파일 ÷ 복구 대상 파일 |
| 중복 절감 가능 용량 | 추가 중복 사본 용량 합계 |
| 폴더 단순화율 | 정리 전후 불필요한 임시 폴더 수 비교 |
| 최상위 파일 감소율 | 정리 전후 최상위에 직접 놓인 파일 수 비교 |

## 14.2 시간 효과

다음 두 작업을 동일한 샘플 폴더로 비교한다.

```text
사람이 직접 분류하고 이동한 시간
AI 제안을 검토하고 적용한 시간
```

추가 측정:

- 최초 분석 시간
- 캐시 사용 재분석 시간
- 사용자 검토 시간
- 적용 시간
- 되돌리기 시간

## 14.3 품질 효과

샘플 문서에 정답 폴더를 미리 지정해 다음을 측정할 수 있다.

```text
AI 1차 추천 정확도
사용자 수정 후 최종 정확도
낮은 신뢰도 문서의 오분류율
규칙 기반 분류 정확도
HWP 파일명 기반 분류 정확도
```

## 14.4 안전 효과

시연 또는 테스트 결과로 다음 수치를 제시한다.

```text
외부 경로 차단 테스트       통과
덮어쓰기 차단 테스트        통과
부분 이동 자동 복구 테스트  통과
변경 파일 감지 테스트       통과
버전 충돌 감지 테스트       통과
```

측정하지 않은 수치를 추정하여 제시하지 않는다.

---

# 15. 평가 항목 대응

## 15.1 현업적용도

평가자가 확인할 내용:

- 실제 업무 문서 형식을 다루는가
- 사용자가 AI 제안을 수정할 수 있는가
- 조직의 표준 폴더 체계를 반영할 수 있는가
- AI 연결이 없을 때도 기본 기능을 사용할 수 있는가
- 설치와 실행이 쉬운가
- 미지원 문서와 오류 파일을 관리할 수 있는가
- 결과 리포트가 제공되는가

강조할 기능:

```text
조직 템플릿
사용자 편집
HWP 최소 대응
분석 전용 모드
원클릭 실행
결과 리포트
```

## 15.2 완성도

평가자가 확인할 내용:

- 정상 상황뿐 아니라 오류 상황도 처리하는가
- 파일이 유실되지 않는가
- 적용 결과가 화면과 일치하는가
- 되돌리기가 실제로 안전한가
- 테스트가 충분한가
- 상태와 로그가 일관적인가

강조할 기능:

```text
경로 보호
트랜잭션 이동
자동 복구
적용 후 재스캔
해시 검증
버전 의존성 검사
자동 테스트
```

## 15.3 혁신성

강조할 내용:

- 외부 AI에 문서를 보내지 않는 로컬 AI
- AI 자동화가 아니라 사람 중심 통제
- 조직 규칙과 AI의 결합
- 중복·유사·관련 문서를 함께 분석
- 적용과 복구까지 연결되는 전체 흐름

## 15.4 사용 편의성

강조할 내용:

- 폴더 선택 방식
- 환경 자동 점검
- 파일별 제안 편집
- 필터와 검색
- 최종 구조 미리보기
- 오류 해결 안내
- 원클릭 되돌리기

## 15.5 효과성

강조할 자료:

- 정리 시간 비교
- AI 추천 수용률
- 사용자 수정률
- 중복 절감 가능 용량
- 적용 및 복구 성공률
- 정리 전후 폴더 구조

---

# 16. 우선순위 로드맵

## P0: 실제 파일 적용 전에 반드시 수정

1. 대상 폴더 이탈 차단
2. 절대경로와 `..` 차단
3. 심볼릭 링크 차단
4. 이동 전 작업 계획 저장
5. 파일별 즉시 로그 기록
6. 중간 실패 시 자동 복구
7. 적용 전 원본 변경 감지
8. 기존 파일 덮어쓰기 금지
9. 실제 이동 개수 기록
10. 적용 후 자동 재스캔
11. 버전 복구 충돌 검사
12. 샘플 생성 시 기존 폴더 무조건 삭제 방지

## P1: 공모전 제출 전에 구현

1. 파일별 AI 제안 수정
2. 조직 표준 템플릿
3. 최종 상태 기반 Before/After
4. AI 제안 이유와 신뢰도
5. HWP/HWPX 최소 대응
6. 분석 실패 및 미지원 상태 구분
7. 결과 대시보드
8. 엑셀 또는 PDF 리포트
9. 실행 환경 점검
10. AI 없이 사용할 수 있는 모드
11. 실제 Ollama 환경 검증
12. 자동 테스트와 CI
13. 저장소 및 배포 파일 정리

## P2: 시범 운영 단계

1. 이미지 PDF OCR
2. 민감정보 포함 가능성 탐지
3. 문서 보존기간 태그
4. 네트워크 드라이브 지원
5. 부서 템플릿 공유
6. 관리자 통계
7. 파일명 표준화 제안
8. 사내 문서관리시스템 연동
9. 대규모 폴더 성능 개선

---

# 17. 공모전 시연 시나리오

## 시연 데이터

업무 문서 30~50개를 사용한다.

포함 항목:

- 최상위에 흩어진 파일
- `새 폴더`, `임시`, `backup_old`
- 완전 중복 파일
- v1, v2, final 문서
- DOCX, XLSX, PPTX, PDF, TXT
- HWP 또는 HWPX
- 이미지형 PDF
- 목적지 충돌 파일
- 파일명이 모호한 문서

## 시연 순서

### 1. 보안 원칙 설명

```text
모든 분석은 로컬에서 수행
외부 API 사용 안 함
사용자 승인 전 파일 변경 안 함
```

### 2. 환경 점검

Ollama와 모델, 폴더 권한이 정상임을 보여준다.

### 3. 스캔

전체 파일, 중복, 미지원, 현재 폴더 구조를 보여준다.

### 4. 템플릿 선택

부서 표준 폴더 템플릿을 선택한다.

### 5. AI 분석

문서 요약과 추천 폴더, 추천 이유를 보여준다.

### 6. 사용자 수정

AI가 잘못 분류한 계약서 한 건을 사용자가 직접 법무 폴더로 변경한다.

### 7. 안전 차단

의도적으로 잘못된 외부 경로를 보여주고 시스템이 차단하는 모습을 보여준다.

```text
../../외부폴더/문서.pdf
```

### 8. 최종 미리보기

이동, 유지, 제외, 충돌 건수를 보여준다.

### 9. 적용

사용자 승인 후 실제 파일을 이동한다.

### 10. 결과

실제 성공 수와 정리 후 폴더 구조를 보여준다.

### 11. 되돌리기

방금 적용한 버전을 되돌리고 원래 상태로 복구한다.

### 12. 효과 수치

```text
수동 정리 시간
AI 보조 정리 시간
중복 파일 수
사용자 수정률
적용 성공률
복구 성공률
```

---

# 18. 제출 산출물

공모전 제출 시 다음 자료를 준비하는 것이 좋다.

## 필수 산출물

- 실행 가능한 프로그램
- 설치 및 실행 안내서
- 프로젝트 소개서
- 기능 명세서
- 시스템 구성도
- 화면 흐름도
- 테스트 결과서
- 안전성 검증 결과
- 샘플 데이터
- 결과 리포트 예시
- 시연 영상
- 발표 자료

## 저장소 정리

- 의미 있는 저장소 이름
- 프로젝트 설명 추가
- 최종 코드를 `main`에 정리
- 초안 PR 정리
- 릴리스 태그
- 화면 이미지
- Before/After GIF
- 테스트 실행 방법
- 알려진 제한사항
- 개인정보 및 캐시 안내
- 라이선스 또는 사용 범위

현재 저장소 이름이 `test1`이고 기능 브랜치가 기본 브랜치로 지정되어 있으므로, 제출 전에 프로젝트 이름과 기본 브랜치를 정리하는 것이 완성도 인상에 도움이 된다.

---

# 19. 프로젝트 소개 문안

## 공식 소개

AI 폴더 정리 도우미는 정리되지 않은 업무 폴더를 분석해 조직의 표준에 맞는 폴더 구조를 제안하는 로컬 AI 기반 문서 정리 도구이다.

문서 내용은 외부 서버로 전송하지 않고 사용자의 PC에서 처리하며, AI는 파일을 직접 이동하거나 삭제하지 않는다. 사용자는 파일별 추천 위치와 분류 이유를 확인하고, 잘못된 제안을 직접 수정한 뒤 최종 계획을 승인할 수 있다.

실제 적용 전에는 대상 폴더 이탈, 동일 파일명 충돌, 원본 파일 변경, 권한 문제 등을 검사한다. 적용 중 오류가 발생하면 이미 이동한 파일을 자동으로 복구하며, 모든 변경 사항은 버전으로 기록되어 이전 상태로 되돌릴 수 있다.

이를 통해 문서 보안을 유지하면서도 반복적인 폴더 정리 업무를 줄이고, 부서별 문서 분류 기준을 일관되게 적용할 수 있다.

---

# 20. 발표 핵심 문장

## 문제

> 업무 폴더에는 동일 문서, 여러 버전, 임시 파일이 계속 쌓이지만, 민감한 사내 문서를 외부 AI에 업로드해 정리하기는 어렵습니다.

## 해결

> 이 도구는 사내 PC에서 로컬 AI로만 문서를 분석하고, 조직 표준에 맞는 정리 계획을 제안합니다.

## 사람 중심 통제

> AI는 파일을 직접 옮기지 않습니다. 사용자가 파일별 제안을 수정하고 최종 승인한 작업만 적용합니다.

## 안전성

> 대상 폴더 밖 경로와 기존 파일 충돌을 사전에 차단하며, 이동 중 문제가 생기면 이미 이동한 파일을 자동으로 복구합니다.

## 현업적용도

> 문서 분류에서 끝나는 것이 아니라 조직 템플릿, 미지원 문서 관리, 결과 리포트, 버전 복구까지 실제 업무 흐름을 지원합니다.

## 효과성

> 정리 시간, AI 제안 수용률, 사용자 수정률, 중복 절감 용량, 적용 및 복구 성공률을 실제 수치로 측정합니다.

---

# 21. 최종 권장안

프로젝트의 발전 방향은 다음과 같이 정리할 수 있다.

```text
AI 폴더 분류 프로토타입
→ 사용자 수정이 가능한 정리 도구
→ 조직 표준을 반영하는 문서관리 도구
→ 안전한 적용과 감사 이력을 갖춘 로컬 문서 거버넌스 도구
```

공모전에서 높은 평가를 받기 위해 가장 중요한 것은 기능의 개수가 아니다. 다음 다섯 가지가 실제로 안정적으로 동작하는지가 핵심이다.

1. AI가 제안한 경로가 잘못되어도 시스템이 차단한다.
2. 사용자가 AI 제안을 파일별로 수정할 수 있다.
3. 이동 중 오류가 발생하면 원래 상태로 자동 복구한다.
4. 조직 표준 폴더 체계를 반복해서 적용할 수 있다.
5. 정리 전후 효과와 모든 변경 이력을 수치와 리포트로 증명한다.

현재 프로젝트는 아이디어와 시연 구조는 충분히 경쟁력이 있다. 다만 현업적용도와 완성도 평가를 높이려면 그래프나 부가 기능을 더 추가하기보다, **경로 보호, 트랜잭션 이동, 사용자 편집, 조직 템플릿, 실제 결과 검증**을 우선 완성하는 것이 가장 효과적이다.
