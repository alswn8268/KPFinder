# K-PathFinder 설치 가이드 — 회사·공공기관 PC 환경별

K-PathFinder는 인터넷 서버 없이 이 PC 안에서만 동작하는 로컬 도구입니다. 클라우드에
"배포"할 곳이 따로 없고, 아래 네 가지 환경 중 내 상황에 맞는 절만 그대로 따라 하면 됩니다.

> 인터넷이 안 되는 PC에서 참고하려면 이 파일 자체를 코드와 함께 옮겨서 오프라인으로 열어보면
> 됩니다. 같은 내용을 브라우저에서 시나리오별로 골라 보고 싶다면, 이 안내를 만들 때 함께
> 공유한 웹 페이지(K-PathFinder 설치 가이드)를 참고하세요.

## 먼저 확인하세요

아래 네 가지만 확인하면 어떤 경로를 타야 할지 정해집니다.

| 확인 항목 | 확인 방법 |
|---|---|
| 이 PC에서 인터넷이 되는가? | 브라우저로 아무 외부 사이트나 열어보기. 안 되면 "망분리(업무망)" PC일 가능성이 큽니다. |
| 관리자 권한이 있는가? | 새 프로그램 설치 시 "관리자 승인" 창이 뜨는지, 또는 IT팀에 계정 권한 확인 |
| Python이 이미 설치돼 있는가? | 명령 프롬프트(cmd)/PowerShell에서 `python --version` — 버전이 나오면 이미 있는 것 |
| git이 설치돼 있는가? (없어도 괜찮음) | `git --version` — 없으면 GitHub에서 zip으로 대신 받으면 됩니다 |

---

## 시나리오 A — 인터넷 O, 관리자 권한 O (가장 쉬운 경우)

가장 표준적인 경로입니다. 설치 프로그램을 그대로 실행하면 됩니다.

1. **Python 3.10 이상 설치** — 이미 있으면 건너뜁니다. [python.org](https://python.org)에서
   설치 파일을 받아 실행할 때 "Add python.exe to PATH" 체크박스를 꼭 켜세요.
2. **코드 내려받기** — git이 있으면 클론, 없으면 GitHub 페이지에서 "Code → Download ZIP"으로
   받아 압축을 풉니다.

   ```bash
   git clone https://github.com/alswn8268/KPFinder
   cd KPFinder
   ```

3. **필요한 패키지 설치**

   ```bash
   pip install -r requirements.txt
   ```

4. **(선택) Ollama로 AI 분류 기능 쓰기** — [ollama.com](https://ollama.com)에서 설치한 뒤
   모델을 받습니다. 건너뛰어도 규칙 기반 분류로 핵심 기능은 그대로 동작합니다.

   ```bash
   ollama pull exaone3.5:2.4b
   ```

5. **실행** — 브라우저가 자동으로 열리며 `http://localhost:8501`에서 바로 사용할 수 있습니다.

   ```bash
   streamlit run streamlit_app.py
   ```

   웹 UI(FastAPI+Vue) 조합을 원하면 Node.js를 추가로 설치하고 `frontend` 폴더에서
   `npm install && npm run dev`를 실행하세요. Streamlit이 준비물이 훨씬 적습니다.

---

## 시나리오 B — 인터넷 O, 관리자 권한 X

설치형 프로그램 대신 "압축만 풀면 쓸 수 있는" 포터블 버전을 씁니다.

1. **포터블 Python 준비** — python.org의 **Windows embeddable package**(zip)를 받아 원하는
   폴더에 풀면 설치 없이 바로 씁니다. pip이 안 들어 있는 버전도 있으니 아래로 pip을 준비합니다.

   ```bash
   # 압축 푼 폴더 안에서
   python.exe get-pip.py
   ```

2. **코드 내려받기** — git 설치도 관리자 권한이 필요할 수 있으니, GitHub에서 zip으로 받는
   편이 간단합니다(Code → Download ZIP → 압축 풀기).
3. **패키지 설치(사용자 폴더에만 설치)** — `--user` 옵션을 쓰면 시스템 폴더를 건드리지 않고
   내 계정 폴더에만 설치됩니다.

   ```bash
   python.exe -m pip install --user -r requirements.txt
   ```

4. **Ollama는 대부분 어려움** — Ollama는 백그라운드 서비스로 등록되는 방식이라 관리자
   권한이 필요한 경우가 많습니다. 안 되면 사이드바에서 "AI 분류 사용" 체크만 끄고 규칙
   기반으로 씁니다 — 스캔·중복탐지·적용·되돌리기는 그대로 다 됩니다.
5. **실행** — 포터블 python으로 그대로 실행하면 됩니다. Streamlit·FastAPI는 사용자 권한
   프로세스로만 동작해 관리자 권한이 필요 없습니다.

   ```bash
   python.exe -m streamlit run streamlit_app.py
   ```

---

## 시나리오 C — 인터넷 X (망분리 업무망 PC)

> **주의**: 이 경로는 기관 보안 정책에 크게 좌우됩니다. 아래는 일반적인 준비 방법이며,
> 실제 반입 가능 여부는 반드시 IT/보안팀에 먼저 확인하세요.

다른 PC에서 미리 준비해 기관의 정식 반입 절차로 옮겨야 합니다.

1. **인터넷 되는 PC(자택 PC 등)에서 준비** — 코드 zip과 포터블 Python(시나리오 B와 동일)을
   받아둡니다.
2. **필요한 패키지를 미리 다운로드** — 업무망 PC와 **같은 OS·같은 Python 버전**으로 받아야
   나중에 그대로 설치됩니다.

   ```bash
   pip download -r requirements.txt -d offline_packages
   ```

3. **기관 반입 절차로 신청** — 코드 + 포터블 Python + `offline_packages` 폴더를 압축해
   자료전송시스템 등 정식 절차로 반입을 신청합니다. 신청 사유에 "외부 네트워크 통신이 없는
   로컬 Python 스크립트"라는 점을 명시하면 검토가 수월할 수 있습니다.
4. **업무망 PC에서 설치 & 실행**

   ```bash
   python.exe -m pip install --no-index --find-links=offline_packages -r requirements.txt
   python.exe -m streamlit run streamlit_app.py
   ```

**참고**
- Ollama(백그라운드 서비스 설치)는 망분리 PC에서 거의 항상 막힙니다. 처음부터 "AI 없이
  규칙 기반 분류만 쓴다"는 전제로 계획하는 것이 현실적입니다.
- 많은 기관이 실행파일(`.exe`/`.msi`)의 반입을 특히 엄격하게 검사합니다. Python 인터프리터
  자체도 소스/포터블 zip 형태로 사전에 반입 승인을 별도 협의해 두는 것을 권장합니다.

---

## 시나리오 D — 아직 잘 모르겠거나, 매우 엄격해 보임

기술보다 제도 확인이 먼저인 상황입니다. IT·보안팀에 아래 세 가지를 구체적으로 문의하면,
그 답에 따라 위 A~C 중 하나로 좁혀집니다.

1. **개인이 만든 Python 스크립트를 업무 PC에서 실행해도 되는지** — 사내 개발 도구 반입/실행에
   대한 정책이 별도로 있는 기관이 많습니다.
2. **가능하다면, 반입 절차와 허용되는 파일 형태가 무엇인지** — 소스코드(.py/.zip)와
   실행파일(.exe/.msi)의 취급 기준이 다른 경우가 흔합니다.
3. **"완전 로컬 실행, 외부 서버 통신 없음"이 심사에 어떻게 반영되는지** — K-PathFinder는
   인터넷 연결 없이 Ollama(로컬 LLM)만 사용하며, 문서 내용이 외부로 전송되지 않습니다. 이
   점을 문의할 때 함께 설명하면 도움이 됩니다.

답을 얻고 나면 A(자유로움) / B(권한만 제한) / C(망분리) 중 맞는 시나리오로 이동해 그대로
따라 하면 됩니다.

---

## 공통으로 알아두면 좋은 것

- **Ollama 없이도 전부 동작합니다.** 사이드바의 "AI 분류 사용" 체크를 끄면 조직 규칙만으로
  스캔·중복탐지·분류·적용·되돌리기가 모두 그대로 동작합니다. AI는 있으면 좋은 보조일 뿐,
  필수가 아닙니다.
- **문서는 어디로도 나가지 않습니다.** 모든 처리는 이 PC 안에서만 이루어지고, 외부 API
  호출이 없습니다. 보안팀에 설명할 때 강조하면 좋은 지점입니다.
